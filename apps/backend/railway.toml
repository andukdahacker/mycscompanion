[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter backend build"

[deploy]
startCommand = "node --import ./apps/backend/dist/instrument.js apps/backend/dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 5
preDeployCommand = "cd apps/backend && pnpm db:migrate"
