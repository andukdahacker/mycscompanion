# ============================================
# mycscompanion Environment Variables
# Copy to .env and fill in values as needed.
# ============================================

# --- Database (Story 1.2) ---
DATABASE_URL=postgresql://mycscompanion:mycscompanion@localhost:5433/mycscompanion

# --- Redis (Story 1.4) ---
REDIS_URL=redis://localhost:6379

# --- Sentry (Story 1.7) ---
# Get DSN from https://sentry.io → Project Settings → Client Keys
MCC_SENTRY_DSN=

# --- Firebase Server (Story 2.1) ---
# Service account JSON (base64 encoded or raw JSON string)
FIREBASE_SERVICE_ACCOUNT=

# --- Firebase Client (Story 2.1) ---
# Get these from Firebase Console → Project Settings → Your Apps → Web App
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# --- Webapp (Story 2.1) ---
VITE_API_URL=http://localhost:3001

# --- Anthropic AI (Story 6.1) ---
ANTHROPIC_API_KEY=

# --- Execution Environment Image (Story 3.1) ---
# Default: registry.fly.io/mcc-execution:latest (prod)
# Local dev: mcc-execution:local (build with: docker compose --profile execution build)
MCC_EXECUTION_IMAGE=mcc-execution:local
# Fly.io app name for execution image registry (Story 3.1)
MCC_FLY_APP_NAME=mcc-execution

# --- Fly.io Execution (Story 3.2) ---
MCC_FLY_API_TOKEN=

# --- Admin (Story 1.7) ---
# Required for Bull Board access at /admin/queues
MCC_ADMIN_USER=admin
MCC_ADMIN_PASSWORD=

# --- General ---
NODE_ENV=development
PORT=3001
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173
LOG_LEVEL=info
